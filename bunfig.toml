[test]
# Default `bun test` / `test:unit` are unit-only. Live smoke lives under
# `src/__tests__/integration` and is run via `bun run test:integration`.
pathIgnorePatterns = ["**/__tests__/integration/**"]
